CREATE SCHEMA PYSTOCK;

CREATE COLUMN TABLE pystock.product(
	pid INT NOT NULL GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
	idate CHAR(20) NOT NULL,
	empid CHAR(5) NOT NULL,
	storeid CHAR(5) NOT NULL,
	barcode CHAR(7) NOT NULL,
	stock CHAR(4) NOT NULL,
	price CHAR(10) NOT NULL
);
